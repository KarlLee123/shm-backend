spring:
  application:
    name: ansteel-shm-backend

  datasource:
    url: jdbc:mysql://localhost:3306/stress_data?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai
    username: root
    password: 1234
    driver-class-name: com.mysql.cj.jdbc.Driver

mybatis:
  mapper-locations: classpath:mapper/**/*.xml
